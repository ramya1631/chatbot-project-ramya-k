package com.example.chatbot.project;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// Indicates this is a Spring Boot application and triggers auto-configuration and component scanning
@SpringBootApplication
public class ChatbotProjectApplication {

	// Main method - entry point of the application
	public static void main(String[] args) {
		// Launches the Spring Boot application
		SpringApplication.run(ChatbotProjectApplication.class, args);
	}

}
