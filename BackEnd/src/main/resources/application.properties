spring.application.name=chatbot-project
# Database config
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect


# ========================
# =  SERVER SETTINGS     =
# ========================
server.port=8080

# ========================
# =  CORS CONFIGURATION  =
# ========================
# This allows React frontend running on localhost:3000 to make API requests
# Custom configuration still needed in code (shown below)
spring.web.allow-cors=true

# ========================
# =  EMAIL CONFIG (Optional) =
# ========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=duttepallar@gmail.com
spring.mail.password=gmna gphk bqhm ltqn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ========================
# =  LOGGING             =
# ========================
logging.level.org.springframework=INFO
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.http.converter.json=DEBUG